I implemented the sensor simulation and text handling by the server, and separated stubs/skeleton for the client and server. I also did output formatting and setting up use of the configs.json file.

A bug I fixed was with the parking server having negative values for the occupied count of a lot.

One design change I made was separating the client and server to have stubs for an RPC layer.

